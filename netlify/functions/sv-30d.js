// scrape-based (same as earlier v2)... shortened header
export default async (req, ctx) => {
  return new Response(JSON.stringify({ error:"placeholder if old function missing" }), { status:200 });
};